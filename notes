
REACT ROUTER 

* HOOKS
  - useParams = se utiliaza para obterner los parametros 



* FUNCTIONAL COMPONENT
  - Navigate =   


  clase 199 - boton de return, estilos 
  clase 201 - animate.css
  clase 203 - query parameters
  clase 205 - mostrar mensajes condicionales

  Navigate es un componente, que nos redirecciona cuando es renderizado. Por otro lado useNavigate es un hook, que nos permite navegar programaticamente de esta manera solo hacemos la navegación bajo determinadas condiciones. Por ejemplo el usuario hace click en un boton.


hook de react
useMemo = memoriza valores
useCallback  = memoriza funciones



sintaxis de bootstrap es: con-pepito-
sintaxis animate.css es: animate__animated seguido de la animecion uqe queramos ej: animate__animated animate__bounce


.col

query parameter
paquete yarn add query-string : se utiliza para extraer todo lo uqe este en un objeto search cl 203





este estilo en css white-space: pre-wrap; me ayuda a realizar un salto de linea en un texto xml con el 'ndondequiero el espacio.


row-cols-sm-3  en hero card Anidación en bootstrap por grupods de filas en una misma linea

scrolltotop: fc que sirve para que el componente se muestre siempre desde el principio de la pag. se debe colocar en la parte alta del router app

text-muted - aclaraa el texto

el archivo _redirects en public, cuando hay error en netifly: lo que hace es decirle a react que haga match con todos los path que se vayan a escribir para que despues los pase por la raiz del proyecto,,, por el DOMINIO, Y  si hay coincidencias con las rutas configuradas en el route, me muestre las paginas correctamente. con un 200